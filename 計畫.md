# YOLO Elf #8 — 多相機支援（詳細實作計畫）

> 唯一剩下的 roadmap 項目，也是唯一的**架構級**改動：從單錄影端走向輕量 NVR。
> 本文件只涵蓋此項目。動工前請先鎖定「產品決策」一節；那是路線分岔，不是實作細節。
> 現況事實均對照 2026-07 當下的程式碼（#1 追蹤、#2 告警、#4 區域、#3 歷史、#7 驗證、#6 指標、#9 export 皆已完成）。

---

## 1. 現況：單相機是「硬綁」的，逐檔說明

| 檔案 | 目前綁定單相機之處 |
| --- | --- |
| `app/stream_state.py` | `StreamHub` 持有**單一** `camera_client: WebSocket \| None`、**單一** `frame_queue(maxsize=1)`、一組整體計數器（`frames_received/processed/dropped`、`last_*`、`total_*`）、單一 `latest_frame`/`latest_detection`。`viewer_clients` 是一個 set，所有 viewer 看同一路。 |
| `app/main.py` | `detection_worker()` 是**單一** loop，只從 `hub.frame_queue` 取；`/ws/camera` 一連上就 `hub.set_camera(ws)`（新錄影端接管舊的）；`/ws/viewer` 加進單一 viewer set；`status_payload()` 回傳單相機快照。 |
| `app/detector.py` | `model.track(persist=True, tracker=...)` 的**追蹤器狀態存在 ultralytics model 物件內**，跨呼叫共用。兩路影格丟進同一 model 物件會**混到彼此的 track_id**。這是最硬的一塊。 |
| `app/zones.py` | `ZoneEngine` 持有**單一**伺服器級 zone 清單；`annotate(detection)` 不帶相機維度。 |
| `app/alerts.py` | `AlertEngine` 單一規則集；冷卻 `_last_fired_monotonic` 以 `rule.name` 為 key（未含相機）。 |
| `app/events.py` | `SightingAggregator._active` 以 `track_id`(int) 為 key，跨相機會**碰撞**；SQLite `sightings` 表**無 `camera_id` 欄**。 |
| `static/phone.js` | 連 `/ws/camera` 不帶 id；一次只有一個錄影端。 |
| `static/viewer.js` | 單一 `<img>`+`<canvas>` 疊圖；假設只有一路。 |

**結論**：`camera_id` 需貫穿「傳輸層 → 偵測 → 追蹤 → 區域 → 告警 → 歷史 → 指標 → UI」每一層。這是路線層級改動，不是加一個 endpoint。

---

## 2. 產品決策（動工前先鎖定）

這些答案會改變架構，請先定：

1. **要不要成為 NVR？** 若只是偶爾第二支手機，成本可能不值得；若要當家用/店面多鏡頭監看，才值得做。
2. **同時路數上限 N？**（建議先鎖 2–4，決定 GPU 排程與 UI 版面。）先量測單卡在目標 `IMG_SIZE` 下的吞吐再定。
3. **每路是否可獨立設定？**（模型 preset、conf、classes、zones、alert rules 是「全域共用」還是「per-camera」？）→ 決定 config 與 API 的維度。**建議**：偵測參數全域共用（省複雜度），但 **zones / alerts / history 一定是 per-camera**（實體位置不同，混用無意義）。
4. **錄影 / 遠端儲存要不要 per-camera？**（`recordings.py`/`remote_storage.py` 目前也是全域。）**建議**第一版錄影仍走既有單路，多相機先只做「即時偵測 + 監看」，錄影延後。
5. **相機身份如何建立？** 由錄影端自報 `camera_id`（`?camera_id=` 或註冊訊息），還是伺服器以環境變數預先定義允許清單？**建議**：環境變數定義 `CAMERAS`（允許清單 + 顯示名），錄影端只能宣告清單內的 id（避免任意 id 灌爆、也讓 UI 有穩定版面）。

> 在上述未定前，不要開始寫。下面的設計以「**建議**」欄的預設值展開，可據實調整。

---

## 3. 設計目標 / 非目標

**目標**
- N 路串流，每路獨立偵測 + 追蹤 + 區域 + 告警 + 歷史。
- 單一 GPU worker 排程多路（**不是每路一 thread 搶 GPU**）。
- Viewer 格狀（九宮格）同時監看；點一路可放大。
- 完全向後相容：未設定多相機時，行為與現在的單相機**逐位元相同**。

**非目標（第一版）**
- 每路獨立偵測參數（preset/conf/classes 全域共用）。
- 多相機錄影 / 遠端上傳（延後）。
- 跨相機的 re-identification（track_id 跨路不共用，也不嘗試關聯）。

---

## 4. 核心資料模型

新增 `StreamChannel`（每路一份狀態），`StreamHub` → `StreamRegistry`（以 `camera_id` 為 key）。

```text
StreamChannel:
  camera_id: str
  display_name: str
  camera_client: WebSocket | None
  frame_queue: asyncio.Queue(maxsize=1)     # 每路各自單槽佇列
  frames_received/processed/dropped: int    # 每路各自計數
  last_* / total_* / latest_frame / latest_detection
  camera_storage_mode / camera_recording

StreamRegistry:
  channels: dict[str, StreamChannel]        # 由 CAMERAS 允許清單預先建立
  viewer_clients: set[WebSocket]            # viewer 可訂閱全部或單一 camera_id
  # 排程：一個共用的「就緒佇列」讓單一 worker 公平取用各路最新影格
```

**排程佇列**：與其讓 worker 去輪詢 N 個 `frame_queue`，用一個共用的 `asyncio.Queue` 收「哪一路有新影格」的訊號（帶 `camera_id`），worker 取訊號→到對應 channel 拿**最新**影格。這樣天然是 round-robin 且維持「只處理最新、丟舊影格」的低延遲特性。

---

## 5. 逐層改動

### 5.1 `app/stream_state.py`（最大改動）
- 抽出 `StreamChannel`（上節）；`StreamHub` 改名 `StreamRegistry`。
- `submit_frame(camera_id, jpeg)`、`publish_detection(camera_id, frame, detection, ...)`、`set_camera(camera_id, ws)`、`snapshot()` 全部帶 `camera_id`。
- `snapshot()` 回傳 `{"cameras": {camera_id: {…per-stream…}}, "camera_ids": [...]}`；同時保留 top-level 聚合欄（總 fps、總 dropped）給既有 UI/指標，避免破壞。
- Viewer 廣播：`publish_detection` 只送給「訂閱該 camera_id 或訂閱全部」的 viewer。

### 5.2 `app/detector.py`（追蹤是最硬的一塊）
- 問題：ultralytics `model.track(persist=True)` 的追蹤器狀態在 model 物件內，多路共用會混 track_id。
- 方案（擇一，建議 A）：
  - **A. Per-camera 追蹤器狀態**：每路各自持有一份追蹤器（ultralytics 支援以 `persist=True` 對「同一 source」續存；多 source 需各自的 tracker 實例）。實作上最穩的是**每路一個 model 實例**（記憶體成本 = N×權重）；或用 ultralytics 較新的多 source 追蹤 API（需驗證版本行為）。
  - **B. 單 model + 手動 tracker**：把追蹤從 ultralytics 抽出來，改用獨立的 ByteTrack 實作，per-camera 各持一份 tracker 狀態，偵測仍走無狀態 `predict`。較大工程，但記憶體只 1×權重、排程乾淨。
- 讓 `detect()` 接受 `camera_id`（或 `tracker_key`），把追蹤狀態導向該路。
- `track_id` 明確**不跨路共用**；UI/歷史都以 `(camera_id, track_id)` 視為唯一。

### 5.3 `app/main.py`
- `detection_worker`：改成從 registry 的共用就緒佇列取 `camera_id`→拿最新影格→`detector.detect(jpeg, frame_id, camera_id=…)`→`zone_engine.annotate(camera_id, detection)`→`sightings.observe(camera_id, detection, now)`→`registry.publish_detection(camera_id, …)`→`alert_engine.process(camera_id, frame, detection)`。
- 單一 worker 即可（GPU-bound）；若量測顯示吃得下，再考慮小批次（把多路同尺寸影格拼 batch 一次推論）。**先單張 round-robin，量測後再優化。**
- `/ws/camera`：接受 `camera_id`（query 或註冊訊息），驗證在 `CAMERAS` 允許清單內；同一 id 的新錄影端接管舊的（維持現有語意，但 per-id）。
- `/ws/viewer`：接受可選 `camera_id`（單看一路）或不帶（看全部/格狀）。
- `status_payload()`：加 `cameras` 維度（見 5.1）。
- 驗證（#7）：WS 已在 handler 內 gate，多一個 `camera_id` 參數不影響 cookie/bearer 檢查。

### 5.4 `app/zones.py` / `app/alerts.py` / `app/events.py`（camera_id 維度）
- **zones**：`ZoneEngine` 改為 per-camera zone 清單：`dict[camera_id, tuple[Zone,...]]`。`annotate(camera_id, detection)`。`GET/POST /api/zones` 加 `camera_id`（相容：不帶時視為 default 相機）。`ZONES` 環境變數格式擴充為可帶 `camera_id`。
- **alerts**：規則可標 `camera_id`（`null`＝套用全部相機）；冷卻 key 改為 `(camera_id, rule.name)`，否則一路觸發會壓住另一路。`process(camera_id, …)`。
- **events**：
  - `SightingAggregator._active` key 改為 `(camera_id, track_id)`；`observe(camera_id, …)`、`take_expired` 產出的 row 帶 `camera_id`。
  - SQLite `sightings` 表**加 `camera_id TEXT` 欄**；`_ensure_schema` 用 `ALTER TABLE ... ADD COLUMN camera_id`（偵測欄位存在與否，舊 DB 平滑升級，舊資料 `camera_id` 為 NULL）。查詢加 `camera_id` 過濾；`/history` 加相機下拉。加 `idx_sightings_camera` 視需要。

### 5.5 `app/config.py`（新設定）
- `CAMERAS`：允許清單 + 顯示名，JSON 或簡易格式，如 `front:前門,back:後院`。空＝維持單相機（default 相機，行為不變）。
- `MAX_CAMERAS`：上限（bounded int，預設 4）。
- 沿用既有 `_list_env`/`_bounded_int_env`/`_choice_env` helper；fail-loud 驗證（重複 id、超過上限即報錯）。

### 5.6 前端 `static/`
- **phone.js**：註冊時帶 `camera_id`（從 `?camera_id=` 或設定頁選）；狀態列顯示自己是哪一路。
- **viewer.js/html**：格狀版面（CSS grid，N 路），每格一組 `<img>`+`<canvas>`（沿用 #5 的 `fitContain`/`drawBoxes`/類別過濾/存圖，per-grid 重用）；點一格放大單看。WS 收多路 frame 時以 `camera_id` 分派到對應格。狀態 chip 與指標 per-camera。
- **demo 模式**：靜態展示要有多相機示意（假的 2–3 路 demo detection），沿用 `?demo=1` 路徑（GitHub Pages 用）。

---

## 6. GPU 排程策略（風險核心）

- 先**量測**：單卡在目標 `IMG_SIZE`/preset 下的每秒可處理張數（用 `scripts/bench_detector.py` 延伸）。N 路的總需求 = Σ 每路 FPS。
- 若吃不下：
  - **動態降 FPS**：每路 request FPS 隨在線路數自動下修（registry 廣播 cap 給各錄影端，沿用 #5 phone 端既有的 adaptiveFps 機制）。
  - **輪流 / 公平佇列**：單槽佇列 + round-robin 已保證「永遠處理最新影格、丟舊的」，忙不過來時自然退化為較低有效 FPS，而不是排隊爆掉。
  - **小批次**：把多路同尺寸影格拼成一個 batch 一次推論（GPU 利用率↑），但會增加尾延遲；先單張，量測後再決定。
- 別每路開一個 thread 各自 `to_thread(detect)`——會多路搶同一 GPU、context 切換成本高。維持**單一** worker 序列化 GPU 存取。

---

## 7. 向後相容 / 遷移

- `CAMERAS` 未設定 → registry 內建一個 `default` 相機，所有既有行為（單 `/ws/camera`、單 viewer、status 欄位、zones/alerts/history）**與現在完全相同**。多相機是「加維度」而非「換行為」。
- SQLite：`ALTER TABLE ADD COLUMN camera_id`，舊資料 NULL，查詢對 NULL 視為 default。
- API：`camera_id` 皆為可選參數，不帶＝default 相機。
- 既有測試（單相機路徑）應全綠不需改；新增多相機測試獨立。

---

## 8. 分階段落地（建議 PR 切分，逐步可上線）

1. **PR1 — Registry 骨架（非破壞）**：`StreamChannel`/`StreamRegistry`，內建單一 default 相機，對外行為零改變。全綠 + 既有測試不動。（= 之前選項「最小非破壞性第一步」）
2. **PR2 — camera_id 貫穿傳輸層**：`/ws/camera`、`/ws/viewer` 帶 id；worker 共用就緒佇列排程；status 加 `cameras` 維度。仍可只有一路。
3. **PR3 — 追蹤 per-stream**：解決 5.2 的追蹤器狀態隔離（先做 A：per-camera model 實例，量測記憶體）。
4. **PR4 — zones/alerts/events 加 camera_id**：含 SQLite migration、API/`/history` 相機維度、alert 冷卻 per-(camera,rule)。
5. **PR5 — Viewer 格狀 UI + phone 註冊 id + demo**：多相機監看，沿用 #5 疊圖/過濾/存圖。
6. **PR6 — GPU 排程優化（量測後）**：動態降 FPS / 小批次，視 PR1–5 的實測數據。

每個 PR 沿用既有慣例：核心邏輯進 `app/`、config 用既有 helper、對外端點 env-only（SSRF）、`pytest` 全綠 + `node --check`、README/TUNING 更新、demo 有對應展示。

---

## 9. 測試策略

- **單元**：`StreamRegistry` 的 per-channel 隔離（兩路計數/佇列不互污）；`SightingAggregator` 以 `(camera_id, track_id)` 不碰撞；alert 冷卻 per-(camera,rule)；zones per-camera annotate；events SQLite migration（舊 schema→加欄）。
- **App/ASGI**（走真實 lifespan）：兩個 `/ws/camera` 帶不同 id 同時在線；`/ws/viewer?camera_id=` 只收該路；`/api/status` 的 `cameras` 維度；`/history?camera_id=` 過濾。
- **相容性**：不設 `CAMERAS` 時，既有單相機測試全數維持。
- **不可 CI 測**：真實多路 GPU 吞吐（需硬體）→ 標記為手動/整合，用 bench 腳本量測。

---

## 10. 風險與未決問題

- **追蹤器狀態隔離**（5.2）是最大技術風險；per-camera model 實例的**記憶體** = N×權重（accurate preset 尤重）。需先量測是否可行，否則走方案 B（自帶 ByteTrack）。
- **GPU 吞吐**（第 6 節）：N 路是否吃得下需實測；可能被迫動態降 FPS。
- **track_id 跨路不可混**；且 `track_id` 在 preset 切換本來就會重置，多路更要謹慎（UI 以 `(camera_id, track_id)` 為唯一）。
- **UI 複雜度**：格狀版面 + per-grid overlay 幾何（letterbox）在不同格尺寸下要正確；沿用 #5 的 `fitContain` 但每格獨立算。
- **範圍蔓延**：錄影/遠端儲存/每路獨立參數若也要 per-camera，工程量再翻倍——建議第一版明確排除（見非目標）。

---

## 11. 沿用既有慣例（摘要）

- 核心邏輯獨立成 `app/<feature>.py`，純函式 + Engine/Registry 類別，`__init__(settings)` fail-loud 驗證。
- `app/config.py` 用既有 `_bool_env`/`_bounded_*`/`_list_env`/`_choice_env` helper，欄位進 `Settings`。
- 對外端點（webhook 等）**只能由環境變數設定**（SSRF 防護）。
- 測試：`tests/test_<feature>.py`（單元）+ `tests/test_app.py`（真實 ASGI lifespan）；app 測試的 autouse fixture 清乾淨相關 env。
- 文件：README（功能列 / 設定表 / API 表 / 專案結構）+ TUNING（操作說明）。
- 驗證：`pytest` 全綠 + `node --check` 改動的 JS；UI 變更盡量瀏覽器實跑（無頭分頁 `visibilityState:hidden` 會暫停 rAF，canvas 需手動驗證繪製幾何）。
